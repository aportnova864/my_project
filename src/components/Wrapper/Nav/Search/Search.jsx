import { NavLink } from "react-router-dom"
const Search = (props) => {
  return (
    <div>
      Search
      <NavLink to="/searchUsers/">Search Users</NavLink>
    </div>
  )
}

export default Search
